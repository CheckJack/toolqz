# Daily follow-up cron (Hostinger VPS — no Vercel cron).
# Replace YOUR_DOMAIN and YOUR_CRON_SECRET, then: crontab -e
#
# 0 8 * * * curl -fsS -H "Authorization: Bearer YOUR_CRON_SECRET" https://YOUR_DOMAIN/api/cron/follow-ups >/dev/null 2>&1
